clc
number = input("enter the %age")
if number >= 80 then
    disp("hackeurr")
elseif number >=70  then
        disp("tatti")
    elseif number >= 60 then
            disp("more tatti")
        else
            disp("tattiest")
        end
