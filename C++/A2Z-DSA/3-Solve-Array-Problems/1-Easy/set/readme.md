# Set in java

- Set doesn't allow duplicate elements.
- Set is an interface that extends Collection interface.
- Set is implemented by HashSet, LinkedHashSet, and TreeSet classes.
- Set on using with HashSet doesn't maintain any order of elements.
    - But TreeSet maintains ascending order of elements so sorted.
- Set doesn't have any index, so we can't access elements by index.
